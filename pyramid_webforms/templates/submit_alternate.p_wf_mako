<fieldset class="submit">
    <input type="submit" name="accept" value="${submit_text}" />
    <span style="margin:0 5px">${or_text}</span>
    <a class="alternate-action" href="${alternate_url}">${alternate_text}</a>
</fieldset>